# 2026-04-15 每日记录

## 数据修正规则

- 当用户提供基金实际持仓份额、实际收益或平台实际确认结果时，以用户提供数据为准。
- 系统按金额/净值推导出的理论份额仅作估算，不一定与平台最终确认完全一致。
- 若用户明确给出实际份额或收益，可直接同步修正 `fund_holdings` 与 `strategy_positions`。
- 若用户下达的是“卖出 X 份”，则卖出逻辑按份额优先；金额只在正式净值确认后回算，不可再用估算金额反推份额。
- 已补上闭环：策略交易一旦确认，需自动同步写入基金主账，避免 `strategy_*` 与 `fund_*` 两套账继续分叉。
- 已新增 `skills/gridseed-v3/morning_audit.py`，每日 09:00 自动巡检一致性；发现 `error/warn` 时通过 Telegram 提醒。
- 历史遗留策略流水已清账：`strategy_trades` 已确认记录与 `fund_trades` 主账记录当前核对为 0 缺失。

